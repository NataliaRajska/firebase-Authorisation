import { MapResultTypePipe } from './map-result-type.pipe';

describe('MapResultTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MapResultTypePipe();
    expect(pipe).toBeTruthy();
  });
});
